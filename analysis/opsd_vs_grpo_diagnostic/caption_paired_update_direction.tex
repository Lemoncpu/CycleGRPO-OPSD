\caption{Paired update-direction diagnostic to be rendered only from a common student checkpoint and a fixed shared rollout. Positions are pre-registered by the update-pre teacher--student gap: group A contains positions where the teacher raises the sampled-token probability, and group B contains positions where it lowers it, after excluding $|\Delta_{\mathrm{teacher}}|\le 0.05$. Each paired group contributes one point with coordinates $(\overline{\Delta\log p}_{A},\overline{\Delta\log p}_{B})$ after one controlled GRPO or OPSD update; arrows connect the origin to the method mean. The current repository has no saved common-rollout log-prob changes, so this figure remains blocked and no simulated path is produced.}
